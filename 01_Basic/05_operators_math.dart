/**
 * Un operador es un simbolo que le dice al compilador que debe de realizar
 * una tarea matematica, relacional o lógica y debe producir un resultado.
 */
void main(){
  int sum = 10 + 5;
  print(sum);
  int sustraction = 20 - 10;
  print(sustraction);
  int multiplication = 10 * 2;
  print(multiplication);
  double division = 10 / 2;
  print(division);
  double residuo = 10 % 3;
  print(residuo);
  int c = 10 ~/ 3; //Division común pero solo la parte entera
  print(c);
}